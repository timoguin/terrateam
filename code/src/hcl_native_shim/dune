(rule
 (target sg_hcl_shim)
 (deps
  (glob_files *.go)
  go.mod
  go.sum)
 (action
  ; -buildvcs=false avoids "go" to do a git operation, that fails because of dubious ownership
  (run go build -buildvcs=false -o %{target} .)))
