(* Replaced at image-build time with the git SHA the image is built from, so a
   shipped binary can be checked against the source it claims to be.  The
   default marks local/dev builds. *)
let sha = "dev"
