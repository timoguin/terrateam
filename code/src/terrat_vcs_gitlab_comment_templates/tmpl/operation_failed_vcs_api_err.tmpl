The `{{ operation }}` call to the GitLab API failed, so Terrateam could not finish this operation.

The usual causes are a GitLab incident, a rate limit, or the Terrateam access token for this project having expired or lost a scope.  Check [status.gitlab.com](https://status.gitlab.com/) and confirm the token is still valid.

Request id `{{ request_id }}`.  Once GitLab is healthy, run the command again.
