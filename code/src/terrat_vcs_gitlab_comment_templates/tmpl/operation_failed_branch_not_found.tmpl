Terrateam asked GitLab for the current commit of `{{ branch }}` and GitLab reports no such branch.  The branch was most likely deleted or renamed while this operation was running.

Running the command again will not help until `{{ branch }}` exists.  If it does exist, check that the Terrateam access token for this project still has access to it.

Request id `{{ request_id }}`.
