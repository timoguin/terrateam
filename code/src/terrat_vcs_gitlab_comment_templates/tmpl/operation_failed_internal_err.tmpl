Stategraph reached a state it does not know how to continue from (`{{ tag }}`) and stopped this operation.  This is not a problem with your configuration.

Running the command again sometimes clears it, because a few of these states come from a race between two operations.

Request id `{{ request_id }}`.  If it happens again, send that request id to support@stategraph.com or ping us on [Slack](https://stategraph.com/slack).
