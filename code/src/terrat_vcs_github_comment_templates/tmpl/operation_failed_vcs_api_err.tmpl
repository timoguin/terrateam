The `{{ operation }}` call to the GitHub API failed, so Terrateam could not finish this operation.

The usual causes are a GitHub incident, a secondary rate limit, or the Terrateam GitHub App losing access to this repository.  Check [githubstatus.com](https://www.githubstatus.com/) and confirm the app is still installed here.

Request id `{{ request_id }}`.  Once GitHub is healthy, run the command again.
