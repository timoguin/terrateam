Stategraph asked GitHub for the current commit of `{{ branch }}` and GitHub reports no such branch.  The branch was most likely deleted or renamed while this operation was running.

Running the command again will not help until `{{ branch }}` exists.  If it does exist, check that the Stategraph GitHub App still has access to this repository.

Request id `{{ request_id }}`.
