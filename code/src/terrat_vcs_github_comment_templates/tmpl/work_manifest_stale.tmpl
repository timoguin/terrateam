## Commits moved during this {% if is_plan %}plan{% else %}apply{% endif %} :warning:

Commits moved while this run operated.
{%- if branch_move %}  The branch was at commit `{{ branch_move.from_sha }}` when the run started.  Its head is now `{{ branch_move.to_sha }}`.{% endif %}
{%- if dest_branch_move %}  The destination branch was at commit `{{ dest_branch_move.from_sha }}` when the run started.  Its head is now `{{ dest_branch_move.to_sha }}`.{% endif %}
{%- if files_unknown %}

Stategraph cannot compare the files of these commits yet, so it cannot tell which directories changed.  Treat this output as stale.
{%- else %}

Files of these directories and workspaces changed after the run started:

| Directory | Workspace |
| --- | --- |
{%- for ds in dirspaces %}
| `{{ ds.dir }}` | `{{ ds.workspace }}` |
{%- endfor %}
{%- endif %}
{%- if is_plan %}

This plan is stale.  Plan again before you apply:

```
stategraph plan
```
{%- else %}

The apply applied the files of commit `{{ run_sha }}`.  {% if files_unknown %}The files of the directories of this apply can be different at the head{% else %}The files of these directories are different at the head{% endif %}, thus the head is not applied.  Plan and apply these directories again.
{%- if is_layered_run %}

The layers that depend on them do not run until the head is applied.  To plan this layer again, comment:

```
{%- for dir in replan_dirs %}
stategraph plan dir:{{ dir }}
{%- endfor %}
```
{%- else %}

```
stategraph plan
```
{%- endif %}
{%- endif %}

[How Stategraph handles commits that move during a run](https://docs.terrateam.io/workflows/stale-runs)
