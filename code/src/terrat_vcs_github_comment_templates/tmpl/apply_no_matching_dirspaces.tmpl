## No Matching Changes :heavy_multiplication_x:

There are no matching changes that are pending apply.
{%- if implicit_and %}

> [!WARNING]
> **Did you mean `or`?** A space between terms in a tag query means `and`, so every term has to be true of the same directory.
{%- if suggestion %}
>
> One directory cannot be two directories at once, which is why nothing matched. To apply all of them, comment:
>
> ```
> terrateam apply {{ suggestion }}
> ```
{%- else %}
>
> Your query was `{{ tag_query }}`. If you meant those terms as alternatives, join them with `or` instead.
{%- endif %}
{%- endif %}

To apply all pending applies, comment:

```
terrateam apply
```
