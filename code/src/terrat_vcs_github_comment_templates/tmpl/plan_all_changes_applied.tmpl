## Nothing to Plan :white_check_mark:

Everything matching your request is already up to date: each directory and workspace either has been applied or its most recent plan found no changes.

To re-plan specific directories anyway, comment:

```
stategraph plan dir:<path>
```
